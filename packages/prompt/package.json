{
  "name": "@tenantguard/prompt",
  "version": "0.0.0",
  "description": "TenantGuard Agent Prompt Compiler — compile safe, scoped agent prompts from a routed queue item",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tenantguard/queue": "workspace:*",
    "@tenantguard/scanner": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
