{
  "name": "@tenantguard/github-app-server",
  "version": "0.0.0",
  "description": "TenantGuard report-only GitHub App — self-hostable deployment runtime (webhook server + concrete GitHub Checks client + ephemeral git workspace)",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tenantguard/github-app": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
