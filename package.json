{
  "name": "tenantguard-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "TenantGuard — CLI-first SaaS Build Kernel (workspace root)",
  "license": "MIT",
  "packageManager": "pnpm@11.0.8",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}
